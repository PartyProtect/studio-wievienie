<section class="fancy-teaser" id="fancy-boogers" aria-labelledby="fancy-teaser-title">
  <div class="shell fancy-teaser-intro">
    <div>
      <p class="eyebrow">Kledinglijn · Studio Wievien</p>
      <h2 id="fancy-teaser-title">Fancy<br />Boogers</h2>
    </div>

    <div class="fancy-teaser-copy">
      <p class="fancy-teaser-lead">Noses, knitted by hand.</p>
      <p>Handmade · made to order · SS26</p>
      <a href="/fancy-boogers/" class="enter-store">Enter storefront <span aria-hidden="true">↗</span></a>
    </div>
  </div>

  <a class="fancy-teaser-door shell" href="/fancy-boogers/" aria-label="Open de Fancy Boogers storefront">
    <figure class="teaser-frame teaser-red">
      <img
        src="/images/fancy-boogers/toile-terror-hero.webp"
        alt="Toile Terror Shirt tegen een rode achtergrond"
        loading="lazy"
        decoding="async"
      />
    </figure>
    <figure class="teaser-frame teaser-green">
      <img
        src="/images/fancy-boogers/boogie-blazer-look-01.webp"
        alt="The Boogie Blazer tegen een groene achtergrond"
        loading="lazy"
        decoding="async"
      />
    </figure>
    <figure class="teaser-frame teaser-purple">
      <img
        src="/images/fancy-boogers/slime-stripe-look-02.webp"
        alt="Slime Stripe look tegen een paarse achtergrond"
        loading="lazy"
        decoding="async"
      />
    </figure>

    <span class="door-label">SS26 · Open Fancy Boogers</span>
  </a>

  <div class="fancy-teaser-ticker" aria-hidden="true">
    <div>
      <span>Noses, knitted by hand · Handmade · Made to order · 1 of 1 · SS26 · </span>
      <span>Noses, knitted by hand · Handmade · Made to order · 1 of 1 · SS26 · </span>
      <span>Noses, knitted by hand · Handmade · Made to order · 1 of 1 · SS26 · </span>
      <span>Noses, knitted by hand · Handmade · Made to order · 1 of 1 · SS26 · </span>
    </div>
  </div>
</section>

<style>
  .fancy-teaser {
    padding-block: clamp(6rem, 10vw, 10rem) 0;
    overflow: hidden;
    background: var(--dark);
    color: var(--on-dark);
  }

  .fancy-teaser .eyebrow {
    color: var(--on-dark-soft);
  }

  .fancy-teaser-intro {
    display: grid;
    grid-template-columns: minmax(0, 1.35fr) minmax(260px, 0.65fr);
    gap: clamp(3rem, 8vw, 9rem);
    align-items: end;
  }

  .fancy-teaser h2 {
    margin-top: 0.8rem;
    font-size: clamp(4.2rem, 8vw, 8rem);
    line-height: 0.76;
    letter-spacing: -0.062em;
  }

  .fancy-teaser-copy {
    padding-bottom: 0.3rem;
  }

  .fancy-teaser-lead {
    font-family: var(--font-display);
    font-size: clamp(1.5rem, 2.5vw, 2.3rem);
    line-height: 1.05;
    letter-spacing: -0.03em;
  }

  .fancy-teaser-copy > p:last-of-type {
    margin-top: 0.9rem;
    color: #8f8b85;
    font-size: 0.62rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .enter-store {
    display: inline-flex;
    gap: 0.5rem;
    margin-top: 1.8rem;
    padding-bottom: 0.18rem;
    border-bottom: 1px solid #69655f;
    font-size: 0.72rem;
    font-weight: 600;
    text-decoration: none;
  }

  .fancy-teaser-door {
    position: relative;
    display: grid;
    grid-template-columns: 1.08fr 0.86fr 1fr;
    gap: clamp(0.55rem, 1.15vw, 1rem);
    align-items: end;
    margin-top: clamp(4rem, 8vw, 7.5rem);
    padding-bottom: clamp(4rem, 7vw, 6rem);
    text-decoration: none;
  }

  .teaser-frame {
    margin: 0;
    overflow: hidden;
    background: #242321;
    transition: transform 650ms var(--ease);
  }

  .teaser-frame img {
    width: 100%;
    aspect-ratio: 3 / 4;
    object-fit: cover;
    transition: transform 850ms var(--ease);
  }

  .teaser-red {
    transform: translateY(0.8rem);
  }

  .teaser-green {
    transform: translateY(-1.8rem);
  }

  .teaser-purple {
    transform: translateY(1.9rem);
  }

  .door-label {
    position: absolute;
    right: 0;
    bottom: 1.1rem;
    color: #8f8b85;
    font-size: 0.6rem;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .fancy-teaser-ticker {
    overflow: hidden;
    border-top: 1px solid #343331;
    border-bottom: 1px solid #343331;
    padding-block: 0.9rem;
  }

  .fancy-teaser-ticker > div {
    display: flex;
    width: max-content;
    color: #8f8b85;
    font-size: 0.6rem;
    font-weight: 600;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    white-space: nowrap;
    animation: fancyTeaserFlow 30s linear infinite;
  }

  .fancy-teaser-ticker span {
    padding-right: 2.2rem;
  }

  @keyframes fancyTeaserFlow {
    to { transform: translateX(-25%); }
  }

  @media (hover: hover) and (pointer: fine) {
    .fancy-teaser-door:hover .teaser-red {
      transform: translateY(-0.25rem) scale(1.012);
    }

    .fancy-teaser-door:hover .teaser-green {
      transform: translateY(-2.8rem) scale(1.018);
    }

    .fancy-teaser-door:hover .teaser-purple {
      transform: translateY(0.6rem) scale(1.012);
    }

    .fancy-teaser-door:hover img {
      transform: scale(1.018);
    }
  }

  @media (max-width: 720px) {
    .fancy-teaser-intro {
      grid-template-columns: 1fr;
      gap: 2.4rem;
    }

    .fancy-teaser h2 {
      font-size: clamp(4.2rem, 20vw, 6rem);
    }

    .fancy-teaser-door {
      grid-template-columns: 1.1fr 0.9fr;
      gap: 0.55rem;
      margin-top: 4rem;
      padding-bottom: 4.2rem;
    }

    .teaser-red {
      grid-row: 1 / span 2;
      align-self: stretch;
      transform: none;
    }

    .teaser-green,
    .teaser-purple {
      transform: none;
    }

    .teaser-red img {
      height: 100%;
    }

    .door-label {
      left: 0;
      right: auto;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .fancy-teaser-ticker > div {
      animation: none;
    }

    .teaser-frame,
    .teaser-frame img {
      transition: none;
    }
  }
</style>
