<div class="assembly-crew" data-assemble="crew" aria-hidden="true">
  <svg viewBox="0 0 220 124" role="presentation" focusable="false">
    <path class="crew-ground" d="M5 112.5 H82" />

    <g class="crew-supervisor">
      <circle cx="17" cy="57" r="6.5" />
      <path d="M17 64 L17 88" />
      <path d="M17 87 L8 108" />
      <path d="M17 87 L28 108" />
      <path d="M17 69 L8 83" />
      <g class="crew-supervisor-arm">
        <path d="M17 69 L30 61 L31 49" />
        <path class="crew-thumb" d="M31 49 L35 52 M31 49 L28 53" />
      </g>
    </g>

    <g class="crew-worker-stage">
      <g class="crew-worker">
        <circle cx="50" cy="43" r="7" />
        <path d="M49 50 L43 78" />
        <path d="M44 77 L27 108" />
        <path d="M44 77 L67 105" />
        <path d="M47 56 L62 64" />
        <path d="M43 58 L60 65" />
      </g>
    </g>

    <g class="crew-rope">
      <path class="crew-rope-slack" d="M61 64 C103 78 158 49 213 31" />
      <path class="crew-rope-taut" d="M61 64 L213 31" />
      <circle class="crew-hook" cx="213" cy="31" r="2.5" />
    </g>
  </svg>
</div>

<style>
  .assembly-crew {
    position: absolute;
    left: clamp(-3.5rem, -3vw, -2rem);
    bottom: -0.4rem;
    z-index: 4;
    display: none;
    width: clamp(9.5rem, 13vw, 12rem);
    color: color-mix(in srgb, var(--ink) 76%, transparent);
    pointer-events: none;
  }

  .assembly-crew svg {
    display: block;
    width: 100%;
    height: auto;
    overflow: visible;
  }

  .assembly-crew circle,
  .assembly-crew path {
    fill: none;
    stroke: currentColor;
    stroke-width: 1.65;
    stroke-linecap: round;
    stroke-linejoin: round;
    vector-effect: non-scaling-stroke;
  }

  .crew-ground {
    opacity: 0.42;
  }

  .crew-rope-slack,
  .crew-rope-taut {
    stroke-width: 1.15;
  }

  .crew-rope-taut {
    stroke-dasharray: 160;
    stroke-dashoffset: 160;
  }

  .crew-hook,
  .crew-supervisor,
  .crew-worker-stage,
  .crew-ground,
  .crew-rope-slack,
  .crew-rope-taut {
    opacity: 0;
  }

  .crew-supervisor-arm {
    transform: rotate(92deg);
    transform-box: view-box;
    transform-origin: 17px 69px;
  }

  .crew-thumb {
    opacity: 0;
  }

  :global(html[data-assembly='pending']) .assembly-crew,
  :global(html[data-assembly='playing']) .assembly-crew {
    display: block;
  }

  /* The rope becomes taut before the word is allowed to move. */
  :global(html[data-assembly='playing'] [data-assemble='wievien']) {
    animation: assembly-wievien-crew-haul 2700ms 1680ms linear both !important;
  }

  /* Nothing downstream gets approved until the supervisor does. */
  :global(html[data-assembly='playing'] [data-assemble='index']) {
    animation-delay: 4940ms !important;
  }

  :global(html[data-assembly='playing'] [data-assemble='door']:nth-child(1)) {
    animation-delay: 5100ms !important;
  }

  :global(html[data-assembly='playing'] [data-assemble='door']:nth-child(2)) {
    animation-delay: 5270ms !important;
  }

  :global(html[data-assembly='playing'] [data-assemble='door']:nth-child(3)) {
    animation-delay: 5440ms !important;
  }

  :global(html[data-assembly='playing'] [data-assemble='door']:nth-child(4)) {
    animation-delay: 5610ms !important;
  }

  :global(html[data-assembly='playing']) .assembly-crew {
    animation:
      crew-enter 700ms 1120ms var(--ease) both,
      crew-exit 650ms 5850ms var(--ease) both;
  }

  :global(html[data-assembly='playing']) .crew-ground {
    animation: crew-ground-in 500ms 1200ms ease-out both;
  }

  :global(html[data-assembly='playing']) .crew-supervisor {
    animation: supervisor-arrive 700ms 1380ms var(--ease) both;
  }

  :global(html[data-assembly='playing']) .crew-supervisor-arm {
    animation: supervisor-thumb 430ms 4580ms cubic-bezier(0.16, 1, 0.3, 1) both;
  }

  :global(html[data-assembly='playing']) .crew-thumb {
    animation: thumb-appear 220ms 4780ms ease-out both;
  }

  :global(html[data-assembly='playing']) .crew-worker-stage {
    animation: worker-arrive 620ms 1240ms var(--ease) both;
  }

  :global(html[data-assembly='playing']) .crew-worker {
    animation: worker-haul 2820ms 1530ms linear both;
    transform-box: view-box;
    transform-origin: 43px 108px;
  }

  :global(html[data-assembly='playing']) .crew-rope-slack {
    animation: rope-slack 640ms 1300ms ease-out both;
  }

  :global(html[data-assembly='playing']) .crew-rope-taut {
    animation:
      rope-tighten 520ms 1590ms ease-out both,
      rope-strain 2400ms 2060ms linear both;
  }

  :global(html[data-assembly='playing']) .crew-hook {
    animation: hook-set 280ms 1500ms ease-out both;
  }

  @keyframes assembly-wievien-crew-haul {
    0% {
      opacity: 0;
      transform: translate(0.72em, 0.08em) rotate(6deg) scale(1.025);
    }
    9% {
      opacity: 0.85;
      transform: translate(0.57em, 0.064em) rotate(4.7deg) scale(1.018);
    }
    17% {
      opacity: 1;
      transform: translate(0.64em, 0.07em) rotate(5.05deg) scale(1.02);
    }
    35% {
      transform: translate(0.37em, 0.041em) rotate(2.65deg) scale(1.012);
    }
    45% {
      transform: translate(0.44em, 0.049em) rotate(3.05deg) scale(1.014);
    }
    64% {
      transform: translate(0.16em, 0.017em) rotate(1deg) scale(1.005);
    }
    73% {
      transform: translate(0.215em, 0.023em) rotate(1.22deg) scale(1.006);
    }
    88% {
      transform: translate(0.04em, 0.004em) rotate(0.16deg) scale(1.001);
    }
    94% {
      transform: translate(0.012em, 0.001em) rotate(0.04deg) scale(1);
    }
    100% {
      opacity: 1;
      transform: none;
    }
  }

  @keyframes crew-enter {
    from {
      opacity: 0;
      transform: translate(-0.35rem, 0.2rem);
    }
    to {
      opacity: 1;
      transform: none;
    }
  }

  @keyframes crew-exit {
    from {
      opacity: 1;
    }
    to {
      opacity: 0;
      transform: translateY(0.2rem);
    }
  }

  @keyframes crew-ground-in {
    from {
      opacity: 0;
      stroke-dasharray: 80;
      stroke-dashoffset: 80;
    }
    to {
      opacity: 0.42;
      stroke-dasharray: 80;
      stroke-dashoffset: 0;
    }
  }

  @keyframes supervisor-arrive {
    from {
      opacity: 0;
      transform: translateY(0.35rem);
    }
    to {
      opacity: 0.72;
      transform: none;
    }
  }

  @keyframes supervisor-thumb {
    0% {
      transform: rotate(92deg);
    }
    68% {
      transform: rotate(-6deg);
    }
    100% {
      transform: rotate(0deg);
    }
  }

  @keyframes thumb-appear {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  @keyframes worker-arrive {
    from {
      opacity: 0;
      transform: translateX(-0.3rem);
    }
    to {
      opacity: 0.88;
      transform: none;
    }
  }

  @keyframes worker-haul {
    0% {
      transform: rotate(-2deg) translateX(0);
    }
    8% {
      transform: rotate(-8deg) translateX(-1px);
    }
    17% {
      transform: rotate(-13deg) translateX(-3px);
    }
    24% {
      transform: rotate(-7deg) translateX(0);
    }
    39% {
      transform: rotate(-15deg) translateX(-4px);
    }
    48% {
      transform: rotate(-9deg) translateX(-1px);
    }
    64% {
      transform: rotate(-16deg) translateX(-5px);
    }
    73% {
      transform: rotate(-10deg) translateX(-2px);
    }
    88% {
      transform: rotate(-18deg) translateX(-5px);
    }
    95% {
      transform: rotate(-9deg) translateX(-1px);
    }
    100% {
      transform: rotate(-6deg) translateX(0);
    }
  }

  @keyframes rope-slack {
    0% {
      opacity: 0;
      transform: translateY(3px);
    }
    35% {
      opacity: 0.62;
    }
    100% {
      opacity: 0;
      transform: translateY(0);
    }
  }

  @keyframes rope-tighten {
    from {
      opacity: 0.15;
      stroke-dashoffset: 160;
    }
    to {
      opacity: 0.82;
      stroke-dashoffset: 0;
    }
  }

  @keyframes rope-strain {
    0%,
    100% {
      transform: translateY(0);
    }
    13% {
      transform: translateY(0.8px);
    }
    21% {
      transform: translateY(-0.4px);
    }
    42% {
      transform: translateY(1px);
    }
    51% {
      transform: translateY(-0.5px);
    }
    71% {
      transform: translateY(0.8px);
    }
    80% {
      transform: translateY(-0.35px);
    }
  }

  @keyframes hook-set {
    from {
      opacity: 0;
      transform: scale(0.4);
      transform-origin: 213px 31px;
    }
    to {
      opacity: 0.78;
      transform: scale(1);
      transform-origin: 213px 31px;
    }
  }

  @media (max-width: 720px) {
    .assembly-crew {
      left: -0.65rem;
      bottom: -1.25rem;
      width: 8.5rem;
      opacity: 0.88;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .assembly-crew {
      display: none !important;
    }
  }
</style>
